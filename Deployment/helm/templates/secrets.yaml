apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: {{ .Values.appName }}-secrets
  namespace: {{ .Release.Namespace }}
spec:
  provider: azure
  parameters:
    clientID: "{{ .Values.clientId}}"
    keyvaultName: "{{ .Values.keyVault.name}}"
    objects: |
      array:
        - |
          objectName: TenantCatalogDatabaseConnection
          objectType: secret
        - |
          objectName: ClerkSecretKey
          objectType: secret
        - |
          objectName: ClerkWebhookSecret
          objectType: secret
        - |
          objectName: GitHubToken
          objectType: secret
    tenantID: "{{ .Values.keyVault.tenantId}}"
  secretObjects:
    - secretName: adminapicredentials
      type: Opaque
      data:
        - objectName: TenantCatalogDatabaseConnection
          key: TenantCatalogConnection
    - secretName: clerkcredentials
      type: Opaque
      data:
        - objectName: ClerkSecretKey
          key: SecretKey
        - objectName: ClerkWebhookSecret
          key: WebhookSecret
    - secretName: githubcredentials
      type: Opaque
      data:
        - objectName: GitHubToken
          key: Token
